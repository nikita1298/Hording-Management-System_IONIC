import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AddpackPage } from './addpack';

@NgModule({
  declarations: [
    AddpackPage,
  ],
  imports: [
    IonicPageModule.forChild(AddpackPage),
  ],
})
export class AddpackPageModule {}
